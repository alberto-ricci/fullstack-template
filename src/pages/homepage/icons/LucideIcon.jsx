import React from "react";
import { Feather } from "lucide-react";

const LucideIcon = ({ className = "w-6 h-6 text-purple-600" }) => (
	<Feather className={className} />
);

export default LucideIcon;
