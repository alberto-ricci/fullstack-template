import React from "react";
import { Boxes } from "lucide-react";

const ComponentIcon = ({ className = "w-6 h-6 text-blue-500" }) => (
	<Boxes className={className} />
);

export default ComponentIcon;
